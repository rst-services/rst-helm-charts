Zabbix Proxy "{{ include "zabbix-proxy.fullname" . }}" foi instalado.

  Image:        {{ .Values.image.repository }}:{{ .Values.image.tag }}
  Persistência: {{ if .Values.persistence.enabled }}{{ .Values.persistence.size }} (PVC){{ else }}emptyDir (efêmero){{ end }}
  Service:      {{ .Values.service.type }} :{{ .Values.service.port }}

Toda configuração do Zabbix (ZBX_*) vem de `extraEnv`. Para inspecionar o env aplicado:
  kubectl -n {{ .Release.Namespace }} get statefulset {{ include "zabbix-proxy.fullname" . }} \
    -o jsonpath='{.spec.template.spec.containers[0].env}' | jq

Próximos passos:

1. No Zabbix Server, cadastre o proxy com o mesmo nome configurado em ZBX_HOSTNAME:
   Administration → Proxies → Create proxy

2. Verifique se o pod está saudável:
   kubectl -n {{ .Release.Namespace }} get pods -l app.kubernetes.io/instance={{ .Release.Name }}

3. Logs:
   kubectl -n {{ .Release.Namespace }} logs -l app.kubernetes.io/instance={{ .Release.Name }} --tail=50 -f
